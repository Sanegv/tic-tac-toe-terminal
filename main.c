#include "fonctions.h"

int main(){
	/*char** grille = init_grille();
	tour(grille, 'X');
	tour(grille, 'X');
	tour(grille, 'X');
	print_grille(grille);
	printf("%d\n", check_victoire(grille));
	free_grille(grille);*/

	jeu();
	return 0;
}